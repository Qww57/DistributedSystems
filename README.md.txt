# Distributed Systems 

This repository is composed of many folders. 
- ComProtocols is a folder containing protocols
- MainSystem part supposed to enable the communication between users.

To build the project, you may have to add the libraries. See the folder lib, right click on it. Go to "Build path" -> "Add to build path"